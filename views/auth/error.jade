!!! 5
html
  head
    meta(charset="utf-8")
    meta(name="viewport",content="width=device-width, initial-scale=1.0")
    title Plunker - Authorization failed to #{auth.service}
  body
    script
      var auth = !{JSON.stringify(auth)};
      if (window.opener && auth) {
        window.opener._handleOAuthError(auth);
      }
      window.close();